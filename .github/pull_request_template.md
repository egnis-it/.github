<!--
제목: [TASK-0000] <타입>: <한 줄 요약>
      타입 = feat | fix | hotfix | refactor | perf | docs | test | chore | revert
      타입은 이 PR 이 내놓는 결과의 성격. 브랜치 안 커밋들의 타입 합이 아니다
      노션 티켓이 없으면 [TASK-0000] 자리를 생략한다

왜             이 변경이 필요한 이유. 티켓에 있으면 링크로 대체
무엇을         바뀐 동작. 파일 목록이 아니라 무엇이 달라지는가
배포 시 주의   해당되는 줄만 남기고 나머지는 지운다. 목록에 없으면 직접 적는다
               전부 해당 없으면 이 절을 통째로 지운다

이 주석과 안 쓴 절은 지운다. 본문은 스쿼시 머지 시 그대로 커밋 메시지가 된다.
-->

## 왜

## 무엇을

## 배포 시 주의

- DB 마이그레이션
- 환경변수 추가 (ECS 태스크 정의 새 리비전 필요)
- 다른 서비스와 배포 순서
